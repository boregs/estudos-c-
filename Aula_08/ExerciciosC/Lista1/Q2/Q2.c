#include <stdio.h>
#include <stdlib.h>

/*Enunciado : O setor de TI precisa emitir alertas sobre a temperatura de um servidor.
Crie um programa que exiba a temperatura atual (pré-definida) em graus Celsius com
duas casas decimais*/

int main()
{
   float temp = 36.6523;

   printf("\n-- Temperatura atual do servidor | %.2f Celsius --\n", temp);
}
