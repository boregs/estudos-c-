#include <stdio.h>
#include <stdlib.h>

int main()
{
    int True = 1;
    int contador = 1;
    int limite = 10;

    while(contador <= limite){ // condiçao para o loop funcionar
        printf("isso e um while loop\n");
        contador += 1; // adiciona + 1 a variavel contador até passar a condição do loop, quando passar ele para de loopar
    }



}
